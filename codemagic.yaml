workflows:
  build-apk:
    name: Build APK
    environment:
      flutter: stable
    scripts:
      - flutter packages pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
      
